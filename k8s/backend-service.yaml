# ClusterIP service: only accessible WITHIN the cluster
# The frontend reaches the backend through this internal service name
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP          # Internal-only (default)
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
